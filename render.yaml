services:
  - type: web
    name: equipment-list-api
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: DATA_DIR
        value: /tmp/equipment-list
      - key: FRONTEND_ORIGIN
        value: "*"
